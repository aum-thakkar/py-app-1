name: Backstage Platform Engg CI/CD pipeline for py-app-1
on:
  push:
    branches:
      - main
    paths:
      - src/**

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Reduce the GITHUB_SHA to 6 chars
        shell: bash
        run: |
          echo "COMMIT_ID=${GITHUB_SHA::6}" >> ${GITHUB_ENV}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          push: true
          platforms: linux/amd64,linux/arm64
          tags: aumthakkar/py-app-1:${{ env.COMMIT_ID }}

    outputs: 
      commit_id: ${{ env.COMMIT_ID }}

  cd:
    needs: ci
    runs-on: orbstack-runner
    steps:
      - name: Checkout GitHub repo
        uses: actions/checkout@v7

      - name: Using yq utility modify the tags from the Helm values file
        run: |          
          # Download and set up yq
          VERSION=v4.44.1
          PLATFORM=linux_arm64
          
          curl -L "https://github.com/mikefarah/yq/releases/download/${VERSION}/yq_${PLATFORM}" -o ./yq 
          chmod +x ./yq
          
          # Update the Helm values.yaml file with the latest commit_sha
          ./yq -i '.image.tag = "${{ needs.ci.outputs.commit_id }}"' charts/py-app-1/values-dev.yaml

      - name: Commit Changes to GitHub
        uses: EndBug/add-and-commit@v10
        with:
          message: "pushed the image with tag: ${{ needs.ci.outputs.commit_id }}"

      - name: Install ArgoCD CLI
        run: |
          VERSION=$(curl -L -s https://raw.githubusercontent.com/argoproj/argo-cd/stable/VERSION)
          curl -sSL -o argocd-linux-amd64 https://github.com/argoproj/argo-cd/releases/download/v$VERSION/argocd-linux-amd64
          sudo install -m 555 argocd-linux-amd64 /usr/local/bin/argocd
          sudo chmod +x /usr/local/bin/argocd
          rm argocd-linux-amd64

      - name: ArgoCD login and App sync
        run: |
          argocd login argocd-server.argocd \
          --username ${{ secrets.ARGOCD_USERNAME }} \
          --password ${{ secrets.ARGOCD_PASSWORD }} \
          --insecure \
          --grpc-web

          # Create and sync the app
          REPO="https://github.com/aum-thakkar/py-app-1.git"
          
          # If not there add the Repo to ArgoCD
          argocd app list | grep -q ${REPO} || argocd repo add ${REPO}

          # Create app if not there already
          if argocd app get py-app-1 > /dev/null 2>&1; then
            echo "Deleting old app - py-app-1 for a fresh new install..."
            argocd app delete py-app-1 -y

            # Wait while the app gets deleted
            while argocd app get py-app-1; do
              echo "Waiting for App py-app-1 to be deleted..."
              sleep 2
            done
            echo "App: py-app-1 deleted."
          fi

          # Create the app
          argocd app create py-app-1 \
          --dest-server https://kubernetes.default.svc \
          --dest-namespace dev \
          --path charts/py-app-1 \
          --repo ${REPO} \
          --revision main \
          --sync-option CreateNamespace=true \
          --sync-policy manual \
          --values values-dev.yaml

          argocd app sync py-app-1
          argocd app wait py-app-1 --timeout 120





  